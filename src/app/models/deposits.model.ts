export interface Deposit{
    matched_citation_count: number
    filename: string
    submitted_at: string
    status: string
    length: number
    pingback_url: string
    citation_count: number
    test: boolean
    owner: string
    batch_id: string
}